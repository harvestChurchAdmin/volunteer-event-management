# Server configuration --------------------------------------------------------
NODE_ENV=development             # change to production when deploying
PORT=3002                        # http port Express should bind to
APP_BASE_URL=http://localhost:3002 # public origin used for OAuth + email links
SESSION_SECRET=change-me         # replace with a long, random string in prod
DB_PATH=./db/volunteer.db        # location of the SQLite database file

# Branding --------------------------------------------------------------------
APP_NAME=Event Sign-up Manager
APP_TAGLINE=Coordinate sign-ups with an accessible experience.
ORG_DISPLAY_NAME=Your Organization
ORG_COPYRIGHT_HOLDER=
BRAND_LOGO_URL=
# If left empty and a file exists at src/public/favicon.ico it will be served.
# You can also point to a static path like /img/favicon.png
BRAND_FAVICON_URL=
BRAND_HOME_PATH=/

# Google OAuth for admin login (required for production) ----------------------
GOOGLE_CLIENT_ID=                # OAuth client id from Google Cloud console
GOOGLE_CLIENT_SECRET=            # OAuth secret paired with the client id
GOOGLE_WORKSPACE_DOMAIN=replace-with-domain # restrict admin logins to this domain

# Volunteer management tokens (in days) ---------------------------------------
MANAGE_TOKEN_TTL_DAYS=30         # expiry window for self-service reservation links

# Mail configuration ----------------------------------------------------------
# Option 1: use a well-known service such as Gmail (requires app-password)
MAIL_SERVICE=gmail
MAIL_USER=replace-with-user
MAIL_PASS=replace-with-app-password

# Option 2: comment out MAIL_SERVICE and configure SMTP host/port directly
# MAIL_HOST=
# MAIL_PORT=587
# MAIL_SECURE=false
# MAIL_USER=
# MAIL_PASS=

# Outgoing email headers ------------------------------------------------------
MAIL_FROM=replace-with-email-email-address
MAIL_REPLY_TO=replace-with-email-address
