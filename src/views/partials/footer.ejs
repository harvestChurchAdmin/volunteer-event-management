    </main>
    <footer class="site-footer">
      <div class="site-footer__inner">
        <div>
          <p class="site-footer__title"><%= (locals.brand && locals.brand.appName) || 'Event Sign-up Manager' %></p>
        </div>
        <div class="site-footer__links">
          <a href="/events">Find Sign-ups</a>
          <% if (user) { %>
            <a href="/admin/dashboard">Admin Dashboard</a>
            <a href="/logout">Logout</a>
          <% } else { %>
            <a href="/auth/google">Admin Login</a>
          <% } %>
        </div>
      </div>
      <div class="site-footer__meta">
        <p>&copy; <%= new Date().getFullYear() %> <%= (locals.brand && locals.brand.copyrightHolder) || 'Your Organization' %>. All rights reserved.</p>
      </div>
    </footer>
    <script src="/js/main.js" defer></script>
    <% const layout = locals.layoutVariant || 'public'; %>
    <% if (layout === 'admin') { %>
      <script src="/js/admin-event-detail.js" defer></script>
    <% } %>
</body>
</html>
